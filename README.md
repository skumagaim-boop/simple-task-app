# Taskly

A small, accessible task manager built with React and TypeScript. Tasks are saved in
`localStorage` and can be created, edited, deleted, completed, and filtered.

## Development

```bash
npm install
npm run dev
```

## Checks

```bash
npm test
npm run build
npm run lint
```
